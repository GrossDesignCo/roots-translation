import { Book } from '@/types';
import job_1 from './job-1';

export const job: Book = {
  meta: {
    name: 'Job',
    translationChain:
      'אִיּוֹב (Iyov/Persecuted) → Ἰώβ (Iōb) → Iob → Job',
  },
  chapters: [job_1],
};
